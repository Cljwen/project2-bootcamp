import { createTheme } from "@mui/material";

export const GlobalTheme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#00685b",
      light: "#439688",
    },
    secondary: {
      main: "#f9f9f9",
    },
  },
});
